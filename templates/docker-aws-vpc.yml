meta:
  instance_type: m1.large
  subnets: (( merge ))

networks:
  - name: default
    type: manual
    subnets: (( meta.subnets ))

resource_pools:
  - name: default
    cloud_properties:
      instance_type: (( meta.instance_type ))

compilation:
  cloud_properties:
    instance_type: c1.medium
